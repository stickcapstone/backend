spring:
  application:
    name: veriweb-backend

  datasource:
    url: jdbc:mysql://mysql-94133f3-ccapstonee.b.aivencloud.com:27160/defaultdb?useSSL=true&requireSSL=true&serverTimezone=Asia/Seoul
    username: avnadmin
    password: AVNS_WdOzgavAFKab5c8Z8E5
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-on-validation-error: true
    clean-disabled: false

server:
  port: 8081

veriweb:
  claude:
    api-key: ${CLAUDE_API_KEY}
    model: claude-sonnet-4-6
  newsapi:
    api-key: ${NEWSAPI_KEY}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
